# 🔧 解决GitHub Actions工作流显示问题

**问题**：在GitHub Actions页面看不到"Deploy to Tencent Cloud COS"工作流

**原因**：工作流文件还没有成功推送到GitHub（因为GitHub推送保护）

---

## ✅ 解决方案

由于GitHub推送保护的问题，工作流文件还没有成功推送。我建议使用以下方法解决：

### 方案1：在GitHub网页上允许密钥（最简单）⭐

**步骤**：

1. **访问GitHub推送保护页面**
   ```
   https://github.com/HMC1234/-yqf-aibtrip-demo/security/secret-scanning/unblock-secret/35kZr13D1u5yIkArn2B6gr3nZKU
   ```

2. **允许密钥**
   - 点击"Allow this secret"按钮
   - 原因：这些只是文档中的示例说明，不是真实密钥
   - 选择"Allow this secret"

3. **告诉我已允许**
   - 允许后，告诉我"已允许"
   - 我会立即帮您推送工作流文件

---

### 方案2：创建新分支推送工作流文件（推荐）⭐

我可以帮您创建一个新分支，只推送工作流文件，然后合并到main分支。

**步骤**：

1. **创建新分支**
   ```bash
   git checkout -b add-workflow
   ```

2. **只添加工作流文件**
   ```bash
   git add .github/workflows/
   git commit -m "feat: 添加腾讯云COS自动部署工作流"
   ```

3. **推送新分支**
   ```bash
   git push origin add-workflow
   ```

4. **在GitHub上创建Pull Request**
   - 访问：https://github.com/HMC1234/-yqf-aibtrip-demo/pulls
   - 创建PR从`add-workflow`合并到`main`
   - 合并PR

**优点**：
- ✅ 不会触发推送保护（新分支）
- ✅ 可以查看工作流文件
- ✅ 通过PR合并更安全

---

### 方案3：手动在GitHub网页上创建文件

您也可以在GitHub网页上直接创建文件：

**步骤**：

1. **访问GitHub仓库**
   ```
   https://github.com/HMC1234/-yqf-aibtrip-demo
   ```

2. **创建`.github/workflows`目录**
   - 点击"Add file" → "Create new file"
   - 输入路径：`.github/workflows/deploy-tencent.yml`
   - 粘贴工作流文件内容（我可以提供）
   - 点击"Commit new file"

3. **创建文件**
   - 文件名：`.github/workflows/deploy-tencent.yml`
   - 内容：我可以为您提供完整的文件内容

---

## 📋 工作流文件内容

工作流文件内容如下（您可以复制到GitHub网页上创建）：

```yaml
name: Deploy to Tencent Cloud COS

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGION: ap-guangzhou
  BUCKET: yqf-aibtrip-demo

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build
        env:
          CI: false

      - name: Setup Python for coscmd
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install coscmd
        run: |
          pip install coscmd

      - name: Configure coscmd
        env:
          SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          BUCKET: ${{ secrets.TENCENT_BUCKET || env.BUCKET }}
          REGION: ${{ secrets.TENCENT_REGION || env.REGION }}
        run: |
          coscmd config -a "$SECRET_ID" -s "$SECRET_KEY" -b "$BUCKET" -r "$REGION"

      - name: Deploy to Tencent COS
        env:
          BUCKET: ${{ secrets.TENCENT_BUCKET || env.BUCKET }}
          REGION: ${{ secrets.TENCENT_REGION || env.REGION }}
        run: |
          echo "开始上传文件到COS..."
          coscmd upload -rs build/ /
          
          echo "设置index.html缓存策略..."
          coscmd put build/index.html /index.html -H '{"Cache-Control":"no-cache, no-store, must-revalidate", "Content-Type":"text/html; charset=utf-8"}'
          
          echo "✅ 文件上传完成"
          
      - name: Deployment Summary
        env:
          BUCKET: ${{ secrets.TENCENT_BUCKET || env.BUCKET }}
          REGION: ${{ secrets.TENCENT_REGION || env.REGION }}
          CDN_DOMAIN: ${{ secrets.TENCENT_CDN_DOMAIN }}
        run: |
          echo "✅ 部署完成！"
          echo "📦 存储桶: $BUCKET"
          echo "📍 地域: $REGION"
          if [ -n "$CDN_DOMAIN" ]; then
            echo "🌐 CDN地址: https://$CDN_DOMAIN"
          else
            echo "🌐 COS地址: https://$BUCKET.cos-website.$REGION.myqcloud.com"
          fi
```

---

## ✅ 文件创建成功后

文件创建成功后，您会看到：

1. **GitHub Actions页面**
   - 访问：https://github.com/HMC1234/-yqf-aibtrip-demo/actions
   - 左侧应该能看到"Deploy to Tencent Cloud COS"工作流

2. **工作流状态**
   - 工作流名称：`Deploy to Tencent Cloud COS`
   - 触发方式：Push和Workflow dispatch（手动触发）

3. **可以手动触发**
   - 点击工作流名称
   - 点击右上角的"Run workflow"按钮
   - 选择分支：`main`
   - 点击"Run workflow"

---

## 🚀 推荐方案

我**强烈推荐使用方案3（手动在GitHub网页上创建文件）**，因为：

- ✅ 最简单直接
- ✅ 不会触发推送保护
- ✅ 可以立即看到工作流
- ✅ 不需要处理Git问题

---

**您想使用哪个方案？或者我可以帮您在新分支上推送工作流文件！** 🚀


