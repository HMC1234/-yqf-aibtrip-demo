name: Deploy to Tencent Cloud COS

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGION: ap-guangzhou
  BUCKET: yqf-aibtrip-demo

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build
        env:
          CI: false

      - name: Setup Python for coscmd
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install coscmd
        run: |
          pip install coscmd

      - name: Configure coscmd
        env:
          SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          BUCKET: ${{ secrets.TENCENT_BUCKET || env.BUCKET }}
          REGION: ${{ secrets.TENCENT_REGION || env.REGION }}
        run: |
          echo "配置coscmd..."
          echo "存储桶: $BUCKET"
          echo "地域: $REGION"
          
          if [ -z "$SECRET_ID" ] || [ -z "$SECRET_KEY" ]; then
            echo "❌ 错误: SecretId或SecretKey未配置"
            echo "请检查GitHub Secrets: TENCENT_SECRET_ID 和 TENCENT_SECRET_KEY"
            exit 1
          fi
          
          echo "开始配置coscmd..."
          coscmd config -a "$SECRET_ID" -s "$SECRET_KEY" -b "$BUCKET" -r "$REGION"
          
          if [ $? -ne 0 ]; then
            echo "❌ coscmd配置失败"
            echo "可能的原因："
            echo "1. SecretId或SecretKey错误"
            echo "2. 存储桶名称或地域错误"
            echo "3. 密钥权限不足"
            exit 1
          fi
          
          echo "✅ coscmd配置成功"

      - name: Deploy to Tencent COS
        env:
          BUCKET: ${{ secrets.TENCENT_BUCKET || env.BUCKET }}
          REGION: ${{ secrets.TENCENT_REGION || env.REGION }}
        run: |
          echo "开始上传文件到COS..."
          coscmd upload -rs build/ /
          
          echo "设置index.html缓存策略..."
          coscmd put build/index.html /index.html -H '{"Cache-Control":"no-cache, no-store, must-revalidate", "Content-Type":"text/html; charset=utf-8"}'
          
          echo "✅ 文件上传完成"
          
      - name: Deployment Summary
        env:
          BUCKET: ${{ secrets.TENCENT_BUCKET || env.BUCKET }}
          REGION: ${{ secrets.TENCENT_REGION || env.REGION }}
          CDN_DOMAIN: ${{ secrets.TENCENT_CDN_DOMAIN }}
        run: |
          echo "✅ 部署完成！"
          echo "📦 存储桶: $BUCKET"
          echo "📍 地域: $REGION"
          if [ -n "$CDN_DOMAIN" ]; then
            echo "🌐 CDN地址: https://$CDN_DOMAIN"
          else
            echo "🌐 COS地址: https://$BUCKET.cos-website.$REGION.myqcloud.com"
          fi
<<<<<<< HEAD
=======

>>>>>>> a356223 (feat: 娣诲姞鑵捐浜慍OS鑷姩閮ㄧ讲鏀寔)
