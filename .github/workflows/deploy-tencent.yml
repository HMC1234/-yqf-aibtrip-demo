name: Deploy to Tencent Cloud COS

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  REGION: ap-guangzhou
  BUCKET: yqf-aibtrip-demo-1387623418

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build
        env:
          CI: false

      - name: Setup Python for Tencent COS SDK
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Tencent COS Python SDK
        run: |
          pip install cos-python-sdk-v5

      - name: Deploy to Tencent COS using Python SDK
        env:
          SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          BUCKET: ${{ secrets.TENCENT_BUCKET || env.BUCKET }}
          REGION: ${{ secrets.TENCENT_REGION || env.REGION }}
        run: |
          echo "开始上传文件到COS..."
          echo "存储桶: $BUCKET"
          echo "地域: $REGION"
          
          # 检查build目录是否存在
          if [ ! -d "build" ]; then
            echo "❌ 错误: build目录不存在"
            exit 1
          fi
          
          # 检查index.html是否存在
          if [ ! -f "build/index.html" ]; then
            echo "❌ 错误: build/index.html不存在"
            exit 1
          fi
          
          # 使用Python SDK上传文件
          python3 << 'PYTHON_SCRIPT'
import os
import sys
from pathlib import Path
from qcloud_cos import CosConfig
from qcloud_cos import CosS3Client

secret_id = os.environ.get('SECRET_ID')
secret_key = os.environ.get('SECRET_KEY')
region = os.environ.get('REGION', 'ap-guangzhou')
bucket = os.environ.get('BUCKET')

if not secret_id or not secret_key:
    print("❌ 错误: SecretId或SecretKey未配置")
    sys.exit(1)

print(f"配置COS客户端...")
print(f"存储桶: {bucket}")
print(f"地域: {region}")

try:
    config = CosConfig(Region=region, SecretId=secret_id, SecretKey=secret_key)
    client = CosS3Client(config)
    print("✅ COS客户端配置成功")
except Exception as e:
    print(f"❌ COS客户端配置失败: {e}")
    sys.exit(1)

# 上传index.html（设置不缓存策略）
print("上传index.html...")
try:
    response = client.upload_file(
        Bucket=bucket,
        LocalFilePath='build/index.html',
        Key='index.html',
        Metadata={
            'Cache-Control': 'no-cache, no-store, must-revalidate',
            'Content-Type': 'text/html; charset=utf-8'
        }
    )
    print(f"✅ index.html上传成功")
except Exception as e:
    print(f"❌ index.html上传失败: {e}")
    sys.exit(1)

# 上传static目录下的所有文件
static_dir = Path('build/static')
if static_dir.exists():
    print("上传static目录...")
    uploaded = 0
    failed = 0
    
    for file_path in static_dir.rglob('*'):
        if file_path.is_file():
            relative_path = file_path.relative_to(Path('build'))
            key = str(relative_path).replace('\\', '/')
            
            # 根据文件扩展名设置Content-Type
            content_type_map = {
                '.js': 'application/javascript',
                '.css': 'text/css',
                '.json': 'application/json',
                '.png': 'image/png',
                '.jpg': 'image/jpeg',
                '.jpeg': 'image/jpeg',
                '.gif': 'image/gif',
                '.svg': 'image/svg+xml',
                '.woff': 'font/woff',
                '.woff2': 'font/woff2',
                '.ttf': 'font/ttf',
                '.eot': 'application/vnd.ms-fontobject',
                '.map': 'application/json',
            }
            
            ext = file_path.suffix.lower()
            content_type = content_type_map.get(ext, 'application/octet-stream')
            
            # 对于JS/CSS等静态资源，设置较长的缓存时间
            cache_control = 'public, max-age=31536000' if ext in ['.js', '.css', '.png', '.jpg', '.jpeg', '.gif', '.svg', '.woff', '.woff2'] else 'public, max-age=3600'
            
            try:
                response = client.upload_file(
                    Bucket=bucket,
                    LocalFilePath=str(file_path),
                    Key=key,
                    Metadata={
                        'Content-Type': content_type,
                        'Cache-Control': cache_control
                    }
                )
                uploaded += 1
                if uploaded % 10 == 0:
                    print(f"  已上传 {uploaded} 个文件...")
            except Exception as e:
                print(f"  ❌ {key} 上传失败: {e}")
                failed += 1
    
    print(f"✅ static目录上传完成: 成功 {uploaded} 个, 失败 {failed} 个")
    
    if failed > 0:
        print(f"⚠️  有 {failed} 个文件上传失败")
        sys.exit(1)
else:
    print("⚠️  static目录不存在，跳过")

# 上传asset-manifest.json（如果有）
manifest_path = Path('build/asset-manifest.json')
if manifest_path.exists():
    print("上传asset-manifest.json...")
    try:
        response = client.upload_file(
            Bucket=bucket,
            LocalFilePath=str(manifest_path),
            Key='asset-manifest.json',
            Metadata={
                'Content-Type': 'application/json',
                'Cache-Control': 'no-cache'
            }
        )
        print("✅ asset-manifest.json上传成功")
    except Exception as e:
        print(f"⚠️  asset-manifest.json上传失败: {e}")

print("✅ 所有文件上传成功")
print("✅ 部署完成！")
PYTHON_SCRIPT

          if [ $? -ne 0 ]; then
            echo "❌ 部署失败"
            exit 1
          fi
          
      - name: Deployment Summary
        env:
          BUCKET: ${{ secrets.TENCENT_BUCKET || env.BUCKET }}
          REGION: ${{ secrets.TENCENT_REGION || env.REGION }}
          CDN_DOMAIN: ${{ secrets.TENCENT_CDN_DOMAIN }}
        run: |
          echo "✅ 部署完成！"
          echo "📦 存储桶: $BUCKET"
          echo "📍 地域: $REGION"
          if [ -n "$CDN_DOMAIN" ]; then
            echo "🌐 CDN地址: https://$CDN_DOMAIN"
          else
            echo "🌐 COS地址: https://$BUCKET.cos-website.$REGION.myqcloud.com"
          fi
