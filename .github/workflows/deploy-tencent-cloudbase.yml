name: Deploy to Tencent CloudBase

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  ENV_ID: your-env-id

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build
        env:
          CI: false

      - name: Install CloudBase CLI
        run: npm install -g @cloudbase/cli

      - name: Deploy to CloudBase
        env:
          TENCENT_SECRET_ID: ${{ secrets.TENCENT_SECRET_ID }}
          TENCENT_SECRET_KEY: ${{ secrets.TENCENT_SECRET_KEY }}
          TCB_ENV_ID: ${{ secrets.TENCENT_CLOUDBASE_ENV_ID || env.ENV_ID }}
        run: |
          # CloudBase CLI 2.10+ 使用环境变量方式认证（非交互式）
          # 设置环境变量后，CLI 会自动使用这些凭证，无需显式登录
          export TENCENT_SECRET_ID=$TENCENT_SECRET_ID
          export TENCENT_SECRET_KEY=$TENCENT_SECRET_KEY
          
          # 验证 CLI 版本
          echo "CloudBase CLI 版本:"
          tcb -v
          
          # 检查环境ID
          if [ -z "$TCB_ENV_ID" ] || [ "$TCB_ENV_ID" = "your-env-id" ]; then
            echo "⚠️  警告: CloudBase 环境ID未配置"
            echo "如果您不需要使用 CloudBase Hosting，可以禁用此 workflow"
            echo "或者请在 GitHub Secrets 中设置 TENCENT_CLOUDBASE_ENV_ID"
            echo ""
            echo "✅ 跳过 CloudBase 部署（主要使用 COS 部署）"
            exit 0  # 退出但不报错，只是跳过
          fi
          
          echo "环境ID: $TCB_ENV_ID"
          echo ""
          
          # 部署到 CloudBase Hosting
          # 新版本 CLI 支持环境变量认证，无需显式登录
          echo "开始部署到 CloudBase Hosting..."
          tcb hosting deploy build -e $TCB_ENV_ID
          
      - name: Deployment Summary
        run: |
          echo "✅ 部署完成！"
          echo "🌐 环境ID: ${{ env.ENV_ID }}"

